import React from 'react'

export default function MainContent(){
    return(
        <div className = "mainContent">
            <h1 className = "heading">Fun facts about React</h1>
            <ul className = "facts">
                <li>React is not a framework in the traditional sense.</li>
                <li>SEO and React development are complementary.</li>
                <li>The Virtual DOM is much faster than the real DOM.</li>
                <li>The Virtual DOM is much faster than the real DOM.</li>
                <li>React has a large and active community.</li>
            </ul>
        </div>
    )
}